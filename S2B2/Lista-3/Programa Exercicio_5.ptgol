Programa Exercicio_5
Var
	Inteiro: num, num_orig, fac;
Inicio
	Escreva("Digite um número:");
	Leia(num);

	num_orig <- num;
	fac      <- 1;

	Enquanto (num > 1)
		fac <- fac * num;
		num <- num - 1;
	Fim enquanto

	Escreva (num_orig, "! = ", fac);
	Se (a=1) Então
Fim.